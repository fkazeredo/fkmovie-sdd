# PrimeNG 21 does not yet declare Angular 22 in its peer range; Angular's library
# compatibility policy supports libs built for the previous major (see spec 0002 notes).
# Remove when PrimeNG ships Angular 22 support.
legacy-peer-deps=true
