---
description: Create an Architecture Decision Record in docs/adr
---

Create an ADR for the decision: $ARGUMENTS

1. Read `architecture/core-principles.md` and `docs/adr/0000-template.md`.
2. Find the next sequential number in `docs/adr`.
3. Write Context (the real problem and constraints), Decision (what was decided and why,
   preserving owner direction over peer preference), Consequences (positive and negative,
   honestly) and Alternatives Considered (with the reason each was rejected).
4. If the decision changes a default in `architecture/`, say so explicitly and propose the
   guideline edit — an exception MUST NOT silently become a new default.
5. Save as `docs/adr/NNNN-<kebab-case-title>.md` with Status: Proposed, and ask me to
   accept or revise it.
